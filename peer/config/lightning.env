# Lightning's own identity-mode mesh listener config. No secrets here --
# identity mode authenticates via `tailscale whois` + peer/roster.json, not
# a bearer token, so unlike keys/peers.env this file is safe to commit.
SELF_NAME=LIGHTNING
SELF_BIND=127.0.0.1:8793
